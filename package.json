{
  "name": "firestore-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "nodemon --watch app app/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "stripe forward": "stripe listen --forward-to localhost:4242/webhook"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "firebase": "^8.6.3",
    "firebase-admin": "^10.0.2",
    "stripe": "^8.216.0"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0",
    "ts-node-dev": "^1.1.6"
  }
}
